# MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/drishyam
# Or MongoDB Atlas: mongodb+srv://<user>:<pass>@cluster0.xxxxx.mongodb.net/drishyam

# Server
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Python ML microservice (FastAPI + PyTorch LSTM). Leave blank while it
# doesn't exist yet — the backend falls back to a local trend estimate
# whenever this is unset or unreachable, so the demo never hard-fails.
ML_SERVICE_URL=
